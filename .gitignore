# Node (TypeScript edition build/test deps). `dist/` is intentionally committed
# — it is the bundled action — so it is NOT ignored here.
node_modules/
npm-debug.log*

# OS / editor cruft
.DS_Store
*.swp
